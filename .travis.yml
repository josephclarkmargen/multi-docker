sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t josephmargen/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=True josephmargen/react-test npm test

after_success:
  - docker build -t josephmargen/multi-client ./client
  - docker build -t josephmargen/multi-nginx ./nginx
  - docker build -t josephmargen/multi-server ./server
  - docker build -t josephmargen/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push josephmargen/multi-client
  - docker push josephmargen/multi-nginx
  - docker push josephmargen/multi-server
  - docker push josephmargen/multi-worker
